package com.ecom.Controller;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.ModelAttribute;
import org.springframework.web.bind.annotation.RequestMapping;

import com.ecom.DAO.ProductDAO;
import com.ecom.Model.Product;

@Controller
public class ProductController {
	
	//injecting Dependecy
	private ProductDAO productDAO;
	
	@RequestMapping("/ProductForm")
	public String ProductForm(Model theModel)
	{   
		Product theProduct = new Product();
		theModel.addAttribute("addProduct", theProduct); 
		return "productform";
	}
	 @RequestMapping("/addProduct")
	 public String addProduct(@ModelAttribute("addproduct") Product theProduct )
	 {
		 
		 return "productform";
	 }
	 
	 @RequestMapping("/saveProduct")
	 public String saveProduct(@ModelAttribute("addproduct") Product theProdut)
	 { 
		   
		 return "productformS";
	 }

}
